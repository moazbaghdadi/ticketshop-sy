# App
PORT=3000
NODE_ENV=development

# Database
DATABASE_URL=postgres://postgres:postgres@localhost:5432/postgres

# CORS (comma-separated origins)
CORS_ORIGINS=http://localhost:4200,http://localhost:4201,https://moazbaghdadi.github.io

# Auth
JWT_SECRET=change-me-in-production
JWT_EXPIRES_IN=7d

# Admin dashboard (used by the invite CLI when printing the acceptance URL)
DASHBOARD_BASE_URL=http://localhost:4201
